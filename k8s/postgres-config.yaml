apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
data:
  POSTGRES_DB: radiosignals
  # Keep PG data in a subdir so the volume root's lost+found doesn't block initdb.
  PGDATA: /var/lib/postgresql/data/pgdata
